Progetto Maven che permette di generare la classi java dallo schema xsd del wns (copiati dalla cartella di rilascio)

Per generare le classi:
1) run as -> Maven Genereate Sources
2) le classi vengono generate nella cartella definita nel pom.xml (parametro outputDirectory)